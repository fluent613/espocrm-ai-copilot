# OpenAI Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# EspoCRM Configuration
# Your EspoCRM API endpoint (usually ends with /api/v1)
ESPOCRM_URL=http://localhost:8080/api/v1
# Generate this in EspoCRM: Administration → Users → [API User] → API Key
ESPO_API_KEY=your-espocrm-api-key-here

# Security Configuration
# Change this to a secure random string for production
FLUENCY_AUTH_TOKEN=your-secure-access-token-change-me
# Flask secret key for sessions (generate a random string)
FLASK_SECRET_KEY=your-flask-secret-key-change-me

# Integration Notes:
# For EspoCRM integration, you can:
# 1. Add a link in EspoCRM navigation to open Copilot in new tab
# 2. Bookmark the Copilot URL for quick access
# 3. Use the API endpoints directly if needed

# Application Configuration (Optional)
# Port to run the application on
PORT=5000
# Host to bind to (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
HOST=0.0.0.0
# Debug mode (set to False in production)
DEBUG=True

# Session Configuration (Optional)
# Directory for session files (will be created if it doesn't exist)
SESSION_DIR=/opt/copilot/sessions
# Session lifetime in days for normal login
SESSION_LIFETIME_DAYS=7
# Session lifetime in days for "remember me" login
REMEMBER_ME_LIFETIME_DAYS=30
